services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8764:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/moto.db
    restart: unless-stopped
